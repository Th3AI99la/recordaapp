import React from 'react';
import { Card as PaperCard } from 'react-native-paper';

// Re-exportando diretamente o PaperCard para manter os tipos corretos
const Card = PaperCard;

export default Card;
